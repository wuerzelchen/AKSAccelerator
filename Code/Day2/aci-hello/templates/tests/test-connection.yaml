apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aci-hello.fullname" . }}-test-connection"
  labels:
    {{- include "aci-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aci-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
